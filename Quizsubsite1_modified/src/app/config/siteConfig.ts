// ============================================================
//  🎨 CẤU HÌNH GIAO DIỆN WEBSITE — chỉnh sửa tại đây
// ============================================================

// ── BACKGROUND ──────────────────────────────────────────────
//  Để đổi ảnh nền:
//  1. Đặt file ảnh mới vào thư mục  src/imports/
//  2. Sửa tên file trong dòng import bên dưới
//  3. Lưu lại — website tự động cập nhật

import backgroundImage from "../../imports/background111.jpg";
// Ví dụ:  import backgroundImage from "../../imports/background-moi.jpg";

export { backgroundImage };

// ── MÀU SẮC CHỦ ĐẠO ────────────────────────────────────────
//  Màu gradient thanh tiêu đề + nút bấm (CSS gradient string)
export const brandGradient = "linear-gradient(135deg, #f59e0b, #fb923c)";

// ── TIÊU ĐỀ QUIZ ────────────────────────────────────────────
export const siteTitle = {
  line1: "HÀNH TRÌNH TUẦN LỄ VĂN HÓA",
  line2: "SĂN DẤU YODY",
};

// ── NÚT BẮT ĐẦU ─────────────────────────────────────────────
export const startButtonText = "Yes yes yesssss!!!!";
